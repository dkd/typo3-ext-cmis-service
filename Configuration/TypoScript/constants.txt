[global]
plugin.tx_cmisservice.settings {
    tables {
        tt_content {
            # Enable content indexing to CMIS?
            enabled = 1
        }
        pages {
            # Enable page indexing to CMIS?
            enabled = 1
        }

    }
    cmis {
        # Hostname of CMIS server
        hostname = localhost
        # Port number of CMIS server
        port = 8080
    }
    stanbol {
        # Hostname of Stanbol semantic engine
        hostname = localhost
        # Port number of Stanbol semantic engine
        port = 9090
    }
}
